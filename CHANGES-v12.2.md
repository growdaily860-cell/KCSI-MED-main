# KCSI-MED v12.2 변경사항

- AI 모델 비교 연구의 기본 조합을 `gpt-4o-mini`와 `gpt-4.1-mini`로 변경했습니다.
- 기존 Cloudflare Worker의 Chat Completions 연결을 그대로 재사용합니다.
- 두 호출이 모두 실패하면 더 이상 `응답 완료`로 표시하지 않습니다.
- Worker 인증(401/403), 모델 권한, 사용 한도(429), 네트워크 및 응답 형식 오류를 결과 카드에서 즉시 안내합니다.
- 전체 호출 실패 시 채점과 투표를 숨기고 설정을 다시 열어 곧바로 재시도할 수 있습니다.
- 사진 선택과 모바일 카메라 촬영, 블라인드 A/B 배정 및 CSV 누적 기록은 그대로 유지됩니다.
