# KCSI-MED v12.12

## 한국어 각인 정답지 바로 불러오기

- `알약번호`, `파일1`·`파일2`, `각인1`·`각인2`, `분할선`을 기존 연구 데이터 계약 열의 별칭으로 인식
- `case_id`가 없고 `알약번호`가 있으면 원문 번호를 보존하면서 `PILL-001` 형식 시험번호를 결정적으로 생성
- 앞면 파일은 필수로 유지하되 뒷면 파일과 뒷면 각인이 모두 비어 있으면 한 면 데이터로 검증
- 뒷면 사진만 있거나 뒷면 각인만 있는 비대칭 행은 오류로 막고, 정상 단면 행은 제공된 면만 채점
- 검증 요약에 기대 이미지 수, 매칭 수, 누락 수, 한 면 행 수를 분리 표시

## 폴더형 ZIP 지연 매칭

- 개별 사진 여러 장 외에 폴더가 든 ZIP 한 개를 데이터셋 사진 묶음으로 사용
- ZIP 내부 경로에서 Unicode NFC basename을 만들고 대소문자 정규화 후 CSV 이미지 열과 매칭
- 디렉터리, macOS 보조 폴더, 숨김 보조 파일, 운영체제 메타데이터, 지원하지 않는 확장자를 이미지 목록에서 제외
- 같은 basename이 둘 이상이면 어느 파일도 임의 선택하지 않고 중복 오류로 표시
- ZIP 선택 시 entry 이름과 크기만 인덱싱하고, 사용자가 고른 배치에 필요한 이미지만 그때 Blob/File로 해제
- ZIP 1GB, 이미지 1,000장, 전체 해제 크기 2GB, 이미지당 30MB 제한과 비정상 압축률 차단 추가

지원하는 ZIP 내부 이미지 형식은 JPG/JPEG, PNG, WEBP입니다. 정답지와 ZIP은 브라우저 안에서만 대조하며 원본 이미지는 누적 연구기록이나 내보내기에 포함하지 않습니다.

## EXIF 방향과 모델 입력 일치

- 이미지를 EXIF-aware 경로로 디코딩하고 방향이 반영된 픽셀을 캔버스에 그린 뒤 전송용 크기로 축소
- 화면 CSS만 돌리는 방식이 아니라 모델에 전달되는 실제 픽셀 방향을 정규화
- 종횡비를 유지하며 긴 변을 최대 1,600px로 한 번만 축소하고, 같은 결과를 네 후보 모델에 사용
- 처리 뒤 `ImageBitmap`과 임시 Object URL을 해제해 큰 데이터셋의 브라우저 메모리 누적을 줄임

## 1–5건 배치와 마지막 나머지 보존

- 검증된 데이터가 한 건만 있어도 비교 화면에 불러올 수 있고, 한 배치는 1–5건으로 실행
- 순차 범위 선택에서 마지막 1–4건을 버리지 않고 `마지막 N건` 배치로 표시
- 무작위 출제도 한 바퀴의 마지막 나머지를 부분 배치로 내보내 전체 항목이 한 번씩 출제되도록 변경
- 사용하지 않는 사례 카드와 채점 행을 숨기고 프롬프트·응답 파싱·누적 통계·CSV 행 수를 실제 배치 크기에 맞춤
- 단면 사례에는 미제공 면을 추측하지 말고 빈 값으로 두라는 안내를 프롬프트에 포함

검증 화면은 `ceil(유효 행 수 / 5)` 배치와 `배치 수 × 4모델` API 호출 계획을 표시하고, 기본 일일 40회 한도를 넘으면 경고합니다. 이 표시는 비용·한도 계획용입니다. 전체 데이터셋을 자동 실행하거나, 다음 배치를 자동 시작하거나, 중단 지점에서 자동 재개하지 않습니다. 사용자가 각 배치를 선택해 불러오고 전송 동의 후 직접 실행해야 합니다.

## 연구 CSV 감사 정보 확대

- 고정 5건이 아니라 실제 사례 수만큼 모델별 행을 생성해 배치당 4–20행 저장
- `pill_id`, 제공 면, 앞·뒤 이미지 참조, 분할선 정답을 원본 배치 CSV에 추가
- `truth_mode`, 각인 유사도, 앞·뒤 방향 판정, 없는 각인 생성 여부를 자동채점 감사 열로 추가
- 기존 판정, 직접 입력한 0–40점, 자동채점 총점, 수동 수정 필드, 투표 출처, 지연시간, 호출 오류 열을 그대로 유지
- Contract v1 표준 CSV, 6시트 XLSX, PDF 보고서와 누적기록 JSON 백업 경로는 유지

## 각인 정답지 채점 분리

- 의약품명·품목 ID가 없는 정답지를 약물 Top-1 오답으로 분류하지 않고 `truth_mode: imprint`로 채점
- `(없음)`은 무각인 `∅`, `(마크)`는 로고 `¤`, `(확인불가)`는 평가 제외 `?`로 구분
- `(마크) 255`·`(마크)P`처럼 로고와 글자가 함께 있는 원문을 로고+문자 정답으로 보존
- 빈 정답 면과 확인불가 면은 각인 분모에서 제외하고, 단면 알약은 알려진 제공 면만 채점
- 무각인 정답 면에 모델이 글자를 만든 횟수를 `invented_imprints`로 별도 기록
- 표준 CSV와 대시보드에 각인 exact/partial, 유사도, CER, 앞뒤 방향, 평가 면 수를 노출

## 배포 메모

- 앱 버전을 `v12.12`, npm 패키지 버전을 `12.12.0`으로 갱신
- 기존 스크립트 로드 순서 계약을 유지하고 `APP_VERSION` 단일 출처로 새 배포 안내 버전을 갱신
- 기존 `/field`, PIN 로그인, Worker quota, 4모델 병렬 호출, 수동 검토·저장 흐름은 변경하지 않음

배포 전에는 다음을 실행합니다.

```bash
npm test
npm run test:browser   # 선택 · Playwright가 준비된 환경
```
