# v11.14 변경 설명

## 왜 바꿨는가

첨부된 실물 사진 테스트에서 같은 분홍 알약이 그룹 모드에서는 `D120`, 한 알 모드에서는 `DT20`으로 판독됐다. 또한 자동 DB 후보의 DUR 경고가 사람 확인 전에 노출됐고, 한 알 사진을 올릴 때 `document.getElementById(undefined).click()` 오류가 반복됐다.

## 반영한 변경

### 1. 자동 결과와 사람 확인을 분리

- 자동 `db/high`도 `식약처 DB 단일 일치 · 실물 확인 전` 상태로 표시한다.
- 자동 후보는 모두 `PENDING_CONFIRM`에 들어간다.
- 조사관이 실물과 식약처 등록 사진을 대조한 뒤 `manual` 상태로 승격된다.
- 확인 전 후보는 DUR, 마약류 경고, 기저질환 추론, 진술↔약물 대조, 종합 소견에 들어가지 않는다.
- 확인 시각과 방법을 TXT·JSON 리포트에 기록한다.

### 2. 업로드 null 오류 수정

- 공통 이벤트 선택자를 `.pick-btn`에서 `.pick-btn[data-target]`으로 좁혔다.
- `data-solo` 버튼은 한 알 모드 전용 핸들러만 사용한다.
- `openFileInput()` 가드를 추가해 대상 input이 사라진 경우 예외 대신 복구 안내를 표시한다.

### 3. 각인·화질 불확실성 보존

- `T↔1`을 혼동문자 후보에 추가했다.
- Vision 응답에 `front_state`, `back_state`, `image_quality`를 추가했다.
- `무각인 확인`, `판독 불가`, `사진 미제공`을 서로 다른 상태로 저장·표시한다.
- 확신도 70% 미만, 흐림/반사 0.6 초과, 알약 잘림은 high를 mid로 낮춘다.
- 양면 각인이 일치해도 모양·색상이 충돌하면 high로 자동 분류하지 않는다.

### 4. 리포트와 접근성

- 앱/DB 버전, 상태, 화질, 사람 확인 일시·방법을 TXT에 추가했다.
- 원본 이미지를 제외한 구조화 JSON 내보내기를 추가했다.
- 진행 상태에 `aria-live`, 파일 입력과 동적 이미지에 접근 가능한 이름을 추가했다.
- 외부 AI 전송 가능성과 브라우저 임시 저장에 대한 안내를 업로드 영역에 추가했다.
- easy_db 적재 진행 문구가 헤더에 누적되던 문제를 수정했다.

## 의도적으로 남겨 둔 후속 작업

다음 항목은 별도 영상처리 모델·정답 라벨·UX 설계가 필요해 이번 패치에 억지로 넣지 않았다.

- 그룹 이미지의 실제 인스턴스 분할 및 ROI별 재확대
- 앞·뒤 알약 Hungarian matching과 사용자의 드래그 교정 UI
- 문자별 확률을 제공하는 전용 각인 OCR 모델
- 원본 증거 이미지의 SHA-256 및 서버 감사 로그
- 기관 정책에 맞춘 암호화·보존기간·접근통제

현재 구현은 LLM이 반환한 여러 판독안과 화질 상태를 보존하고, 최종 사용을 사람 확인 뒤로 미뤄 위 기능이 추가되기 전의 오확정 위험을 줄인다.

## 테스트

```bash
npm install
npm test
```

`tdz`, DOM 로드, 안전 게이트, 업로드 선택자, 상태 모델, `T↔1`, 기존 각인·색상 회귀를 검사한다.
