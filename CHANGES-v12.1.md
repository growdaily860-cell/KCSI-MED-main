# KCSI Copilot v12.1

## OpenAI 저비용 비교 연습

- 기본 비교 모델을 `GPT-5.6 Luna`와 `GPT-5 mini`로 설정
- 비교 1회가 모델별 1회, 총 2회 API 호출이라는 비용 안내 추가
- `저비용 연습`에서 이미지 `detail: low`, 최대 출력 1,200 토큰 적용
- 작은 각인 평가를 위한 `정밀 비교`에서 이미지 `detail: high`, 최대 출력 2,200 토큰 적용
- 비용 모드를 연구기록과 CSV에 함께 저장
- 다른 제공자와 별도 Worker/API 키 설정은 고급 항목으로 정리

## 사진 등록

- 앞면·뒷면에 눈에 띄는 `사진 선택` 버튼 추가
- 모바일용 `카메라 촬영` 버튼과 후면 카메라 힌트 추가
- 업로드 사진을 최대 1,600px JPEG로 최적화하고 미리보기·삭제 상태 표시
- 파일 형식과 30MB 크기 제한 오류 안내 추가

## 검증

- OpenAI 기본 모델, 비용 모드별 요청 본문, 사진 업로드 UI, CSV 비용 모드 기록 자동검사 추가
