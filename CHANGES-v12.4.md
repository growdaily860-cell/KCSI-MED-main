# KCSI-MED v12.4

## 4개 OpenAI 모델 배치 비교

- 기본 모델을 GPT-4o, GPT-4.1, GPT-5.6 Luna, GPT-5.6 Terra로 변경
- 모델 4개를 블라인드 A–D에 무작위 배정
- 한 모델에 사진 10장과 동일 프롬프트를 한 번에 전송하여 배치당 총 4회 호출
- 모델별 계정 접근 불가, 일일 한도, 인증 만료 오류를 결과 카드에 구분 표시

## 알약 5개·사진 10장

- `1앞, 1뒤 … 5앞, 5뒤` 순서로 사진 10장 일괄 선택
- 휴대폰·태블릿 카메라에서 각 앞·뒷면을 개별 촬영 가능
- 전송 전에 각 사진을 최대 1,280px JPEG로 최적화하고 전체 요청 크기를 제한
- 알약별 익명 시험번호, 선명도, 제품명·앞뒷면 각인 정답지 입력

## 평가와 연구데이터

- 알약 5개 각각 정확성 0/20/40점 평가
- 모델별 근거 타당성 25점, 환각 억제 20점, 명확성 15점과 평균 정확성을 합산한 100점 총점
- 누적 모델 정확도와 이미지 선명도별 정확도 제공
- 배치당 `5개 알약 × 4개 모델 = 20행` CSV 생성
- 기존 단일 알약 A/B 기록과 충돌하지 않는 v2 브라우저 저장 형식 사용

## Worker

- GPT-4.1 및 GPT-5.6 Luna/Terra/Sol 허용 목록 추가
- 5개 구조화 응답을 위해 연구 모드 최대 출력 한도를 5,000토큰으로 확대
- 응답 헤더 `X-KCSI-Worker-Version`으로 배포 버전을 확인할 수 있도록 추가
- 비밀정보를 포함하지 않는 읽기 전용 `/health` 배포 점검 주소 추가
- PIN 로그인, 24시간 세션, 일일 호출 한도와 API 키 서버 보관 방식은 유지
