# KCSI-MED v12.8

- 제공된 ZIP의 개인정보 탐지 규칙을 실제 의료기록 업로드 흐름에 융합했습니다.
- OCR이 이메일을 불완전하게 읽어도 `@`가 포함된 영역과 인접 조각을 자동으로 가립니다.
- `성명`·`이름` 라벨과 값이 서로 다른 OCR 구조로 분리되어도 같은 시각적 행의 이름을 가립니다.
- 표에서 멀리 떨어진 다른 열은 이름 값으로 오인하지 않도록 열 간격 경계를 적용했습니다.
- 긴 표 문서는 겹치는 구간으로 나눠 확대 OCR하고, 전화·팩스 숫자 행은 제한된 보조 OCR로 다시 확인합니다.
- 제공된 `gpt_example.png`를 웹 흐름에서 실행해 Python 결과와 같은 자동 가림 9개를 확인했습니다.
- 모든 자동 처리는 브라우저 안에서 수행되며 원본은 비식별화 전에 서버로 전송하지 않습니다.
