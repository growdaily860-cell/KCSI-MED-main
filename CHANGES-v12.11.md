# KCSI-MED v12.11

## Research Platform v1 통합

- GroundTruth, ResearchInput, ResearchResult, ModelProvider Contract v1
- CSV/TSV/XLSX/XLS/텍스트 PDF와 스캔 PDF 로컬 OCR 정답지 검토
- OpenAI, Anthropic, Gemini, Mock Provider Registry와 공통 오류 정규화
- Dataset → Runner → Provider → Scoring → Result Dataset 파이프라인
- 제품명, 앞/뒤 각인 CER, Brier loss, 응답 완성도, 비용, 강건성 자동채점
- 기존 Arena 결과와 새 Provider Adapter 결과의 Contract v1 호환 변환
- Contract Dashboard, 표준 CSV, 6시트 XLSX, PDF 인쇄용 연구 보고서
- 원본 이미지, base64, provider raw 응답을 보고서에서 제외
- 기존 `/field`, PIN 로그인, 24시간 세션, quota, 200회×2 충전, OpenAI Worker 경로 유지

## Arena 100점 평가표 자동채점 통합

- `scoring/arena-rubric.js` 자동채점(정확성 40 · 근거 25 · 환각 억제 20 · 명확성 15)
- 산정 근거 공개, 사람 수정 시 `manual_override` 기록, 1점 이내 동률 추천
- 정답 누락 시 자동채점 보류, 정답지는 API로 보내지 않고 브라우저에서만 채점
- 정답 상태를 `state.dataset.loadedRows` 하나로 합치고 Contract v1 `answer`/`condition` 동시 제공
- 자동채점 감사 열(`rating_source`, `evaluation_version`, `automatic_total_score`,
  `rating_override_fields`, `vote_source`)을 기존 CSV에 유지
- Contract 보고서 경로(Bridge · Result Dataset)와 자동채점 산식은 서로 독립
- 외부 러너용 `evaluation/promptfoo-assertion.js`를 같은 자동채점 산식 위에 재구성
  (`agent/automate-arena-evaluation`의 별도 100점 산식은 병합하지 않음)

## 고정 샘플 확장과 무작위 출제

- 식약처 공식사진 고정 샘플 확장 세트 120건(사진 240장)·240건(사진 480장) 추가
- 세 세트가 `20 ⊂ 120 ⊂ 240` 으로 포개져 작은 세트 결과를 큰 세트에서 이어 볼 수 있음
- `npm test`가 저장소의 모든 샘플 팩을 검사 — ZIP 해시·사진 해시·정답지 검증·번호 연속성
- 모양·색상·약효분류 쏠림 상한을 둔 결정적 품목 선정 (`scripts/select-mfds-sample-items.mjs`)
- 사진이 빠진 품목을 건너뛰고 목표 건수를 채우는 빌더 (후보 150건 → 목표 120건)
- 이미지 분할을 sharp 우선·ImageMagick 대체로 이중화, 둘 다 없으면 안내 메시지
- ZIP 생성을 외부 `zip` 명령 없이 Node만으로 수행(윈도우에서 마지막 단계 실패 방지)
- `/research`에 🎲 랜덤 5건 뽑기 — 같은 바퀴 중복 없이 전량 순회, seed로 재현 가능
- 무작위 배치 ID에 `-RND<seed>-<회차>`를 남겨 같은 문제를 다시 뽑을 수 있게 함

## 비식별화 성능 수치

- 문서 처리기록 추가 — 문서마다 자동/부분수동/전부수동/실패로 분류해 브라우저에 누적
- OCR 신뢰도를 보존(예전에는 TSV에서 파싱만 하고 버렸음)하고 소요시간·해상도·조건을 함께 기록
- 기록에는 숫자와 라벨만 남기고 이미지·인식 글자·개인정보 값·파일명은 남기지 않음
- 표본 수와 조건을 반드시 포함하는 성능 문장 생성 (`KCSI_DEID.docLogSentences()`)
- 성능 측정용 합성 의료문서 생성기 — 서식 3종 × 촬영조건 8종, 정답지(항목 종류·좌표) 포함
- 문서 비식별화 채점기 — 항목 단위 재현율과 과잉 가림 배수를 함께 산출
- `docs/DEIDENTIFICATION_PERFORMANCE.md`에 정의·산출·인용 규칙 정리
- `/deid-report` 성능 보고 화면 추가 — 실사용 기록과 합성 문서 채점을 한 화면에서 확인
- 합성 문서 배치 측정 실행기 — 사람 확인 창 없이 자동 탐지만 태우고 사본은 만들지 않음
- 한 문서가 실패해도 배치를 멈추지 않고 분모에서 빼지 않음

## 알약별 정확성 직접 채점

- 알약마다 판정 드롭다운 옆에 0~40 점수칸 추가 — 판정을 고르면 40/20/0이 채워지고 그 값을 고칠 수 있음
- 고친 점수가 배치 총점·누적 정확도·CSV `accuracy_score`에 반영되고, 판정은 `verdict` 열에 그대로 남음
- 자동채점과 다르게 고치면 `rating_source`가 `manual_override`, 고친 알약은 `rating_override_fields`에 기록
- 점수칸을 비우면 판정 배점을 쓰므로 이미 저장된 기록은 계산이 달라지지 않음

## 누적 연구결과 저장과 지표 설명

- 저장 실패를 사실대로 알림 — 저장공간 부족·시크릿 모드를 구분해 표시(예전에는 조용히 사라졌다)
- 저장공간이 모자라면 오래된 배치를 덜어내고 저장하되 덜어낸 건수를 함께 알림
- 저장 전 공급자 원본과 자동채점 근거 문장을 제외해 배치당 용량 약 25% 감소
- 누적기록 JSON 백업·복원 추가 — 복원은 덮어쓰지 않고 합치며 같은 배치를 두 번 세지 않음
- 대시보드에 저장 현황(배치 수·용량·남은 여유·마지막 저장 시각) 표시
- 지표 15종의 산출 근거·의미·해석 주의를 화면 패널과 `docs/RESEARCH_DASHBOARD_METRICS.md`에 공개

## 의료인 성명 가림과 비식별화 지표 교체

### 규칙 — 의사·약사 이름을 가린다

- 담당의사·처방의사·주치의·집도의·판독의·진료의·검사자·조제약사·담당약사 뒤의 이름을 성명으로 탐지
- `의사`·`약사`만 있을 때는 콜론이 붙은 경우에만 인정 — "의사 표시", "의사 소견"을 가리지 않기 위해
- 값은 흔한 성씨로 시작하고 뒤에 한글이 더 이어지지 않을 때만 성명으로 판정
- "담당의사 서명", "검사자 확인" 같은 서식 용어는 단어가 거기서 끝날 때만 제외해
  `서명수` 같은 실제 이름은 계속 잡음
- OCR 상자 경로에서도 긴 직함을 먼저 매칭 — `판독의`가 앞서면 "판독의사 조은결"에서
  이름 대신 직함이 가려졌다
- 이 규칙이 없던 동안 합성 문서 배치의 **항목 재현율 상한이 88.2%**(600/680)로 묶여 있었다.
  규칙 추가 후 **100%**(680/680)

### 지표 — "완전 비식별화"를 주 지표에서 내림

- 주 지표를 **항목 재현율**로 교체. 문서 단위 지표는 항목 7개 중 6개를 가린 문서와
  하나도 못 가린 문서를 똑같은 실패로 세어 개선이 숫자에 나타나지 않았다
- **고위험 항목 재현율** 추가 — 주민등록번호·개인식별번호를 전체 평균에서 분리
- **문서당 평균 누락 항목 수**와 **누락 항목 수 분포**(0 / 1 / 2개 이상) 추가
- **완전 비식별화 → 누락 0건 문서** 로 이름 변경. 예전 이름은 통과한 문서가 법적으로
  안전하다는 뜻으로 읽혔다
- 표본이 없을 때 0%가 아니라 "없음"으로 보고

### 상한선 — 누락의 원인을 가른다

- `scoring/rule-ceiling.js` 추가. 정답지의 라벨·값을 이미지 대신 글자로 규칙에 직접 넣어
  "OCR이 완벽했다면 몇 %를 잡았을까"를 계산
- `/deid-report`에 **규칙 상한 X% / 실측 Y%** 와 그 격차를
  **규칙 공백**(100 − 상한, 규칙을 고쳐야 준다)과 **OCR 손실**(상한 − 실측, 촬영·해상도로 준다)로 분리 표시
- 규칙이 못 잡는 칸을 라벨 이름과 건수로 표시("보호자 3건")
- 정답지 스키마 v2 — 항목마다 `label`·`value` 저장(값은 전부 합성값)
- `scripts/backfill-doc-answers.mjs` — 커밋된 팩의 이미지를 다시 만들지 않고 정답지만 채움.
  새로 계산한 좌표가 기존 좌표와 한 픽셀이라도 다르면 아무것도 쓰지 않고 멈춘다
- 라벨·값이 없는 옛 정답지에서는 상한 0%를 지어내지 않고 계산 불가로 표시

## /deid-report 직접 테스트와 합성 세트 설명

### 직접 테스트

- `/deid-report`에 문서 한 장을 넣어 **지금 규칙이 무엇을 잡는지** 눈으로 보는 패널 추가
- 파일 선택과 촬영 입력, 촬영 조건 선택, 가려질 영역을 덮어 그린 미리보기, 종류별 탐지 수
- **가린 사본은 만들지 않는다** — 화면에 그렇게 적고 실제 비식별화는 `/field`로 안내
- 상자를 하나도 못 찾으면 미리보기 설명이 "원본 그대로입니다"로 바뀜
  (아무것도 못 가린 문서를 가려진 것처럼 보여주면 안 된다)
- 이 화면은 찾은 것만 보여줄 뿐 못 찾은 것은 알려주지 못한다는 사실을 함께 표시

### 처리기록 출처 분리

- 처리기록에 `source` 필드 추가 — `field`(실사용) / `report_test`(직접 테스트)
- 실사용 수치는 `/field` 기록만 센다. 같은 문서를 열 번 돌릴 수 있는 직접 테스트가
  분모에 들어가면 시험 삼아 눌러 본 것이 성능으로 둔갑한다
- 직접 테스트 건수는 "위 수치에서 빠져 있습니다"로 따로 표시
- 출처 필드가 없던 옛 기록은 실사용으로 센다(하위호환)
- CSV에는 둘 다 나가고 `source` 열로 구분 — 내보내기에서 조용히 빠지는 기록은 없다
- 요약 문장이 어느 통을 요약한 것인지 밝힘("실사용 기준" / "직접 테스트 기준")

### 합성 문서 세트 구성 설명

- `deident/synthetic-profile.js` 추가. 불러온 정답지에서 세트 구성을 직접 뽑는다
  (화면에 적어 두면 팩을 바꿔도 설명은 그대로 남아 조용히 거짓말이 된다)
- **촬영 조건별 변형 방식** — 음영 합성 / 기하 왜곡 / 픽셀 잡음 / 해상도 축소로 분류.
  "접힘"을 실제 종이 왜곡으로 오해하지 않게 한다. 회전 각도·축소 배율도 표시
- **서식별로 무엇을 합성했나** — 칸(라벨)마다 종류·건수·값 가짓수·합성값 예시.
  같은 "성명"이라도 환자 이름과 담당의사 이름은 다른 칸이므로 따로 센다
- **주민등록번호 안전성을 값으로 검증** — 정답지에 실린 값의 월·일이 존재할 수 없는
  조합인지 직접 센다. 실재할 수 있는 조합이 있으면 그 값을 짚어 경고
- **이 세트로 말할 수 없는 것** — 값 가짓수가 적은 칸(담당의사 4종, 주소 3종),
  음영으로 흉내 낸 조건, 정답지가 개인정보 항목만 담는다는 사실
- 정답지 스키마 v2에 촬영 조건 분류(`kind`)·회전 각도·축소 배율 추가.
  커밋된 팩은 `npm run backfill:docs`로 이미지를 다시 만들지 않고 갱신

### 고침

- 측정 전에도 "측정결과 CSV" 버튼이 보이던 문제 — `display`를 지정한 컨테이너가
  브라우저 기본 `[hidden]{display:none}`을 덮어쓰고 있었다
- 조건 이름을 이어 붙인 문장의 조사가 어긋나던 문제("저조도은" → "저조도는")

## 정답지를 못 읽을 때 원인을 알려준다

현장 알약 사진으로 비교를 돌리려던 사용자가 정답지 단계에서 막혔다. 화면은
"정답지 머리글을 인식하지 못했습니다"만 말하고 무엇이 잘못됐는지 알려주지 않았다.

- 오류에 **파일 첫 줄에서 실제로 읽은 이름**과 **필요한 열 이름**을 함께 담는다.
  몇 개가 인식됐는지도 적어, 3개에 못 미친 이유가 드러나게 했다
- 직접 만든 정답지에서 흔한 표기를 열 이름으로 인정한다 —
  `검체번호` `샘플번호` `sample_id` / `앞면파일` `앞면` `image_front` /
  `뒷면파일` `뒷면` `image_back` / `약품명` `품명` `정답` `product_name` /
  `앞면표기` `전면각인` / `뒷면표기` `후면각인`
- 정답지가 실패한 상태에서 사진을 올리면 오류 메시지가 **덮여 사라지던** 문제를 고쳤다.
  정답지가 풀릴 때까지 이유를 계속 보여준다

### 고침 — 오류 알림을 읽을 수 없던 문제

`.arena-status.error`가 앱 전역 `.error` 스타일에 색과 여백을 빼앗겨 **빨강 배경 위
진한 빨강 글씨**로 나오고 있었다. 정작 원인을 적어 둔 줄이 안 읽히는 상태였다.
글자색·여백을 되돌리고, 상태 알림이 버튼으로 오해되지 않도록 왼쪽 정렬에 말머리 점을
붙였다.
## 각인 정답지로 4모델을 비교한다

현장 사진의 각인을 사람이 직접 적어 만든 정답지에는 정답 약 이름이 없다. 지금까지는
그런 정답지를 통째로 막았다. 하지만 "각인을 얼마나 정확히 읽는가"와 "무각인 알약에서
글자를 지어내는가"는 그 자체로 정당한 연구 질문이고, 그 정답지가 바로 그 데이터다.

- 정답 제품명이 없어도 **각인 정답이 있으면 채점**한다. 정확성 40점을 각인 일치로 매긴다
  (유사도 90% 이상 정답 · 60% 이상 부분 · 그 미만 오답, 앞뒤가 바뀌어도 인정)
- **무각인 면에 없는 글자를 지어내면 오답**으로 처리하고 `invented_imprints`로 센다.
  각인 정답지를 만드는 이유가 이 숫자다
- 채점 결과에 `truth_mode`(`drug`/`imprint`)를 남긴다. 이 값이 없으면 각인 판독 수치가
  약물 식별 정확도로 인용될 수 있다
- 각인 입력 도구의 표기를 해석한다 — `(없음)`은 무각인, `(마크)`는 로고면(글자만 비교),
  `(마크) 255`는 숫자만, `(확인불가)`는 **채점에서 제외**. 괄호를 떼지 않으면 무각인
  알약에서 모델이 옳게 답해도 틀린 것으로 세게 된다
- 판정할 수 없는 면 하나가 그 알약의 점수를 절반으로 깎던 문제도 함께 고쳤다

검증 화면은 각인 정답지를 통과시키되 "각인 판독을 채점하며 약물 식별 정확도는 나오지
않습니다"라고 알린다.

## 배포 전

```bash
npm run build:research
npm test
npm run test:browser   # 선택 · Playwright 설치 시 실제 Chromium 확인
npm run build:samples  # 식약처 접속 가능한 환경에서 고정 샘플 ZIP 생성
```

확장 샘플 120건 ZIP은 저장소에 커밋된 뒤에야 화면에서 불러올 수 있다.
자세한 내용은 `docs/RESEARCH_SAMPLE_SETS.md` 참고.

Anthropic/Gemini 실호출은 아직 운영 Worker upstream에 연결하지 않았다. 공통 provider
프록시에 기존 세션·quota·allowlist를 적용한 뒤 Worker Secret을 추가해야 한다.
